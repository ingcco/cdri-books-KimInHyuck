# 화면 레이아웃 실측 — certicos Books (Figma REST API)

> 출처: Figma REST API 실측 (lastModified 2024-10-02). **2026-07-08 전 화면 재실측**(auto-layout gap/padding·프레임상대 좌표·per-character 색 override 포함).
> **review-ui / Playwright DOM 정합의 SOT** — 재페치 대신 이 문서에서 대조한다. 기준: **1920×1080 고정, 반응형 미고려**.
> 콘텐츠 컬럼 = **960px 중앙정렬** → content-left = **480 @1920**. (일부 아트보드가 476/516으로 미세 오차 → 주 레퍼런스 `18:805` 기준 480 채택.)

## 프레임 목록 (canvas `0:1`)

| id | 이름 | 크기 | 용도 |
|---|---|---|---|
| `18:805` | 도서 검색 결과 화면 | 1920×1758 | 홈 결과(**주 레퍼런스**) |
| `18:969` | 도서 검색 초기 화면 | 1920×1080 | 홈 초기/빈결과 |
| `18:608` / `18:495` | 상세/전체 검색 | 1000×1761 | 주석 프레임(팝오버·타이핑 상태) |
| `744:156` | pop | 360×160 | 상세검색 팝오버(실측 SOT) |
| `744:313` | 내가 찜한 책 화면 | 1920×1758 | 찜 목록 |
| `757:1435` | 내가 찜한 책 초기 화면 | 1920×1758 | 찜 빈 상태 |
| `753:96` | Style | — | 색·타이포 토큰(tokens.md) |
| `3004:621/622` | like/line·like/fill | 24×24 | 하트 아이콘 |

## 기획 주석

- ✅ 유의사항: "디자인이 곧 스펙. 명시되지 않은 요구사항은 지원자 판단." → 픽셀 대조 우선.
- 검색 히스토리 = **`Frame 77`(node `3000:647`)에 디자인 있음**(2026-07-09 PAT 재실측 — 기존 "디자인 없음"은 프레임 누락 오기). **입력창과 이어진 하나의 연속 박스**: 박스 480w · **r24** · `#F2F4F6`(접힘=pill r100 → 펼침=r24). 입력 행 위 = 아이콘 30(벡터 20 `#353C49`)·placeholder 16/500 `#8D94A0`. 히스토리 행: 텍스트 16/500 `#8D94A0`, **행 간격 40**, 좌측 x≈51(입력 텍스트 정렬). 삭제 X: **24×24 탭 / X 16×16 `#222222`**, 우측 인셋 ~25. (별도 떠있는 드롭다운 ❌)
- chevron 크기(PAT 재실측): 상세보기 버튼 = **14×8 `#B1B8C0`**(구현: 20×20 svg를 `size-7`→glyph 14.7×8.4), 상세검색 필터 드롭다운 = **10×6**(`size-5`→glyph 10.5×6).

---

## 실측 정합 SOT (요소 → px)

### ① Header (1920×80, white)

| 요소 | 실측 |
|---|---|
| 로고 "CERTICOS BOOKS" | 24/700 `#353C49`, **x=160**, 수직중앙 |
| 탭1 "도서 검색" | 20/500 `#353C49`, **x=767** |
| 탭2 "내가 찜한 책" | 20/500 `#353C49`, **x=902** (탭 gap **56**) |
| 활성 언더라인 | **1px** `#4880EE`, 탭 폭 전체, 텍스트 하단 ~2px(y=56) |

> **active/inactive 탭 색 동일**(`#353C49`) — 구분은 **언더라인만**. (현 구현의 inactive `text-subtitle`는 오답.)

### ② 검색 영역 (18:805)

| 요소 | 실측 |
|---|---|
| h1 "도서 검색" | 22/700 `#1A1E27`, x=480 |
| 검색 인풋(pill) | 480×50, r100, `#F2F4F6`, pad[10,0,10,10] |
| 검색 아이콘 | 30×30 컨테이너(벡터 20×20 `#353C49`), 아이콘→placeholder gap **11** |
| placeholder | 16/500 `#8D94A0` · 입력 텍스트 `#222222` |
| 상세검색 버튼 | 72×35, r8, stroke `#8D94A0` 1px, pad[5,10,5,10], 14/500 `#8D94A0` |
| 인풋→상세버튼 gap | **16** |
| h1→검색행 gap | **16** |
| 검색행→카운트 gap | **~25** |

### ③ 카운트 (18:805)

| 요소 | 실측 |
|---|---|
| "도서 검색 결과" | 16/500 lh24 `#353C49` |
| "총 N건" | 16/500 lh24, "총 "·"건" `#353C49` + **숫자 `#4880EE`**(per-char override) |
| 라벨 gap | **16** |
| 카운트→리스트 gap | **36** |

### ④ BookListItem — collapsed (960×100)

| 요소 | 실측 |
|---|---|
| pad-left → 썸네일 | **48**, 썸네일 48×68 |
| 하트 오버레이 | 16×16, 썸네일 우상단 flush |
| 썸네일→제목프레임 gap | **48** |
| 제목 / 저자 | 18/700 `#353C49` / 14/500 `#6D7582`, gap **16**, 수직중앙 |
| price | 18/700 `#353C49` |
| 구매하기 | 115×48, r8, `#4880EE`, 16/500 `#FFFFFF`, pad[13,20,13,20] |
| 상세보기 | 115×48, r8, `#F2F4F6`, 16/500 `#6D7582` + chevron 14×8 `#B1B8C0` |
| 버튼 gap / pad-right | **8** / **16**, 버튼 수직중앙 |
| divider | 960×1 `#D2D6DA` (하단) |

### ⑤ BookListItem — expanded (960×344)

| 요소 | 실측 |
|---|---|
| pad-left → 썸네일 | **54**, 썸네일 210×280 (하트 24×24 우상단, `#E84118`) |
| 썸네일→본문 gap | **32** |
| 제목 / 저자 | 18/700 **lh26** `#353C49` / 14/500 lh22 **`#8D94A0`**(subtitle), gap 16 |
| 책소개 라벨 / 본문 | "책 소개" 14/700 lh26 `#353C49` / 본문 10/500 **lh16** `#353C49`, 360 wide, 라벨→본문 gap **12** |
| 가격블록(우측, x=819 w121) | 원가: 라벨 10/500 `#8D94A0` + gap8 + **18/350(→300) 취소선** `#353C49` / 할인가: 라벨 10/500 `#8D94A0` + gap8 + 18/700 `#353C49`. 원가→할인가 gap **8** |
| 상세보기 / 구매하기 | 115×48 우상단(y26) / **240×48** 우하단(y256), 둘 다 right edge x=944(pad-right **16**) |
| 할인가 미노출 | sale_price === -1 이면 할인가 블록 숨김 |

### ⑥ 상세검색 팝오버 (744:156, 360×160)

| 요소 | 실측 |
|---|---|
| 팝오버 | 360×160, r8, `#FFFFFF`, pad 좌우 **24** |
| close | 20×20 우상단(@332,8), 내부 X 12×12 `#B1B8C0` |
| 필터 드롭다운 | **borderless** 100×36, 14/**700** `#353C49` + chevron 10×6 `#B1B8C0`, 언더라인 100×1 **`#D2D6DA`** |
| 검색어 input | 208×36, placeholder 14/500 `#8D94A0`, 언더라인 208×1 **`#4880EE`** |
| 드롭다운↔input gap | **4**, 언더라인 baseline 동일 |
| 행→검색하기 gap | **16** |
| 검색하기 | 312×36, r8, `#4880EE`, 14/500 `#FFFFFF` |

### ⑦ EmptyState (NoData 160×120)

| 요소 | 실측 |
|---|---|
| 아이콘 | 80×80 |
| 아이콘→문구 gap | **24** |
| 문구 | 16/500 `#6D7582` — 홈 "검색된 결과가 없습니다." / 찜 "찜한 책이 없습니다." |
| 배치 | 콘텐츠 영역 중앙 |

### ⑧ Favorites (744:313 / 757:1435)

| 요소 | 실측 |
|---|---|
| h1 "내가 찜한 책" | 22/700 `#1A1E27` |
| 카운트 | "찜한 책" 16/500 + "총 N건"(숫자 `#4880EE`), gap 16 |
| 리스트 | **BookListItem 재사용**(④/⑤ 동일 스펙) |
| 빈 상태 | NoData "찜한 책이 없습니다." |

---

## 비-토큰 색 (실사용, 전역 토큰 아님 — arbitrary value)

`#D2D6DA`(divider·비활성 언더라인) · `#B1B8C0`(chevron·close 아이콘) · `#1A1E27`(페이지 h1). 근거: tokens.md §1 "비-토큰 색".
