---
name: zoom-out
description: 코드 영역이 낯설거나 전체 구조가 이해되지 않을 때 한 레벨 위 추상화 맵을 요청한다. "이 코드 모르겠어", "어떻게 동작해", "한 레벨 위", "맵 그려줘", "구조 파악" 키워드 시 사용. skill-hint가 자동 제안.
effort: medium
---

이 코드 영역이 낯설다. 한 레벨 위로 올라가서 관련 모듈과 호출자들의 맵을 그려줘.

구체적으로:

- 이 코드와 연결된 상위 모듈/컴포넌트
- 주요 호출자 (누가 이걸 쓰는가)
- 데이터 흐름 방향 (입력 → 처리 → 출력)
- 핵심 의존성 (import 관계)

파일 경로와 함수명은 실제 코드에서 확인 후 제시. 추측 금지.
